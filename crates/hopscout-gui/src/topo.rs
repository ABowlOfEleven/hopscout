//! Topology view (placeholder — implemented in the topology-DAG pass).

use hopscout_core::Session;

pub fn show(ui: &mut egui::Ui, _session: &Session) {
    ui.weak("Topology view coming up next.");
}
